<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsa.dao.UserMapper">

    <!-- 下划线列名 -> 驼峰属性由 map-underscore-to-camel-case 自动完成 -->
    <select id="findByUsername" resultType="com.jsa.entity.User">
        SELECT id, username, password, nickname, role, create_time
        FROM user
        WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.jsa.entity.User">
        SELECT id, username, password, nickname, role, create_time
        FROM user
        WHERE id = #{id}
    </select>

</mapper>
