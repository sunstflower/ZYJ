<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsa.dao.SportMapper">

    <select id="findAll" resultType="com.jsa.entity.Sport">
        SELECT id, name, description, create_time
        FROM sport
        ORDER BY id
    </select>

    <select id="findById" resultType="com.jsa.entity.Sport">
        SELECT id, name, description, create_time
        FROM sport
        WHERE id = #{id}
    </select>

</mapper>
